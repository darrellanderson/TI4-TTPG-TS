import { AbstractUI } from "../abstract-ui/abtract-ui";
export declare class DivUI extends AbstractUI {
    constructor(scale: number, scaledLength: number, orientation: "horizontal" | "vertical");
}
