import { LayoutObjects } from "ttpg-darrell";
export declare class LayoutUnitBoxes {
    private readonly _layout;
    constructor(playerSlot: number, numCols?: number);
    getLayout(): LayoutObjects;
}
