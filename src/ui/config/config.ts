import { Color } from "@tabletop-playground/api";

export const CONFIG = Object.freeze({
  FONT_SIZE: 12,
  SPACING: 4,
  BUTTON_WIDTH: 180,
  BUTTON_HEIGHT: 36,
  DARKER: new Color(0.0625, 0.0625, 0.0625, 1),
});
