import { GameObject } from "@tabletop-playground/api";

/**
 * Add a glowing effect to a token.
 * Emission mask is fixed in the template, but we can add glowing lines.
 */
export class GlowingToken {
  constructor(_token: GameObject) {}
}
